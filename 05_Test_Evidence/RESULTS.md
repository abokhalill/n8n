# Test evidence — mandatory edge cases

Generated by `node 05_Test_Evidence/run-edge-cases.mjs` against the running stack.

**14 of 14 cases passed.**

Each case resets the database and mock harness first, so cases are independent.
Assertions are made against external observations — the provider call journal, the
CRM mock record count — rather than against what the pipeline reports about itself.

| # | Case | Result |
|---|---|---|
| 1 | The same lead arrives from WhatsApp and the website within 2 minutes | PASS |
| 2 | A phone number is valid but submitted in two different formats | PASS |
| 3 | The enrichment API times out twice, then succeeds | PASS |
| 4 | The AI model returns an empty or malformed response | PASS |
| 5 | The AI says 'High Potential' but deterministic rules say low value | PASS |
| 6 | The CRM API returns 429 Rate Limit | PASS |
| 7 | The CRM create succeeds, but the workflow times out before confirmation | PASS |
| 8 | The WhatsApp send is retried after a transient error; no duplicate message | PASS |
| 9 | A salesperson is assigned, then becomes unavailable before follow-up | PASS |
| 10 | A lead opts out while a delayed follow-up execution is already scheduled | PASS |
| 11 | A meeting booking webhook is delivered twice | PASS |
| 12 | A manager rejects a VIP lead after the automated qualification step | PASS |
| 13 | A corrupted CSV row is included in a batch of otherwise valid records | PASS |
| 14 | A workflow execution is manually re-run after partial success | PASS |

---

## Case 1 — The same lead arrives from WhatsApp and the website within 2 minutes

**PASS**

- **pass** — two source events produced two lead records
- **pass** — the pair was linked, not left separate `master=01KZXYW2YFAG6HAX0J6P69R1HK merged=01KZXYW3KR9HH2N1FG7SDVWA3Z`
- **pass** — auto-merge tier on a decisive identifier match `tier=auto_merge conf=0.95`
- **pass** — cross-source was recorded as a supporting signal
- **pass** — exactly one CRM record exists `1 records`
- **pass** — the customer was messaged once, not twice `delivered=1`
- **pass** — the superseded record enqueued no downstream effects

## Case 2 — A phone number is valid but submitted in two different formats

**PASS**

- **pass** — all three submissions were accepted
- **pass** — every raw form parsed successfully
- **pass** — all three collapse to one E.164 value `+971501234321`
- **pass** — the raw input is preserved alongside the normalised value

## Case 3 — The enrichment API times out twice, then succeeds

**PASS**

- **pass** — the first two attempts were deferred, not failed outright `deferrals: 1, 2`
- **pass** — backoff attempts are numbered in order
- **pass** — the third attempt succeeded `status=ok`
- **pass** — enrichment data was applied to the lead `company_size=850`
- **pass** — the lead was scored and routed normally afterwards `score=82 -> qualified`

## Case 4 — The AI model returns an empty or malformed response

**PASS**

- **pass** — both leads were still processed
- **pass** — both degraded to a declared fallback
- **pass** — the malformed body is diagnosed precisely `empty_body / malformed_json`
- **pass** — the empty body is diagnosed precisely
- **pass** — scoring is unaffected — the AI never fed it
- **pass** — an unusable model response never manufactures a conflict

## Case 5 — The AI says 'High Potential' but deterministic rules say low value

**PASS**

- **pass** — rules scored the lead low `score=32`
- **pass** — the model disagreed confidently
- **pass** — the disagreement was flagged as material
- **pass** — the lead routed to manual review, overriding its band `disposition=manual_review`
- **pass** — the reason is recorded with its predicate version `AI says qualified at 0.91, rules say unqualified — 2 bands apart (v1)`
- **pass** — no sales outreach was queued for a lead under review `welcome`
- **pass** — the acknowledgement is still sent, as for any other lead `welcome`
- **pass** — no follow-up sequence was started for a lead under review

## Case 6 — The CRM API returns 429 Rate Limit

**PASS**

- **pass** — the 429 was seen and classified as rate limiting `{"event_type":"odoo.failed","decision":"retry","status":"429","reason":"rate_limited","retry_after":"2"}`
- **pass** — the provider's Retry-After was honoured over our own backoff `retry_after=2s`
- **pass** — it was retried rather than dead-lettered
- **pass** — the record was eventually created exactly once
- **pass** — the lead was linked to its CRM record

## Case 7 — The CRM create succeeds, but the workflow times out before confirmation

**PASS**

- **pass** — exactly one CRM record exists despite the lost acknowledgement `1 records`
- **pass** — the pipeline reconciled instead of guessing `{"event_type":"odoo.reconciled","decision":"already_created","note":"create had landed; only the acknowledgement was lost"}`
- **pass** — the audit says plainly what happened
- **pass** — the existing record was adopted, not recreated
- **pass** — the funnel still advanced afterwards `stage=qualified`

## Case 8 — The WhatsApp send is retried after a transient error; no duplicate message

**PASS**

- **pass** — the provider was called twice `2 POSTs`
- **pass** — the first call was rejected with the injected 503 `status=503`
- **pass** — exactly one message was actually delivered `delivered=1`
- **pass** — the claim settled as succeeded
- **pass** — the retry was counted rather than hidden `attempt=2`

## Case 9 — A salesperson is assigned, then becomes unavailable before follow-up

**PASS**

- **pass** — the breach was detected `40 minutes elapsed`
- **pass** — the owner was re-checked at dispatch, not trusted from assignment
- **pass** — it escalated to a manager `mgr_dana -> dana@example.test`
- **pass** — ownership actually moved, not just a logged recommendation `rep_amara -> rep_yuki`
- **pass** — the reason records how the new owner was chosen
- **pass** — the new owner starts on a fresh SLA clock

## Case 10 — A lead opts out while a delayed follow-up execution is already scheduled

**PASS**

- **pass** — a follow-up was genuinely scheduled first
- **pass** — the sequence stopped when it came due
- **pass** — it stopped for the right reason `consent withdrawn`
- **pass** — no follow-up message was sent after the opt-out `before=1 after=1`
- **pass** — nothing was left queued to fire later

## Case 11 — A meeting booking webhook is delivered twice

**PASS**

- **pass** — the provider delivered twice
- **pass** — the first delivery applied
- **pass** — the second was recognised as a duplicate
- **pass** — the lead moved to booked exactly once
- **pass** — the CRM shows one record reaching meeting_booked `new -> qualified -> meeting_booked`
- **pass** — marketing follow-up stopped
- **pass** — the rep was notified once, not twice `before=1 after=2`
- **pass** — both deliveries are visible in the audit, one suppressed

## Case 12 — A manager rejects a VIP lead after the automated qualification step

**PASS**

- **pass** — the lead was gated as VIP
- **pass** — the transactional acknowledgement was still sent `welcome`
- **pass** — no sales outreach was sent while approval was pending
- **pass** — the rejection was applied once
- **pass** — a second click changed nothing
- **pass** — the lead is recorded as rejected with a reason
- **pass** — the funnel rolled back explicitly, not silently `new -> qualified -> contacted`
- **pass** — no retraction message was sent to the customer `before=1 after=1`

## Case 13 — A corrupted CSV row is included in a batch of otherwise valid records

**PASS**

- **pass** — the batch was accepted rather than rejected wholesale
- **pass** — all five rows were seen
- **pass** — the three valid rows imported `imported=3`
- **pass** — the two corrupt rows were quarantined
- **pass** — each rejection names its line and its fault `line 3: column_count_mismatch; line 6: malformed_quoting`
- **pass** — the good rows went all the way through scoring
- **pass** — quarantined rows wait for a human rather than being retried
- **pass** — re-importing the identical file imports nothing new
- **pass** — and does not queue the same broken rows again

## Case 14 — A workflow execution is manually re-run after partial success

**PASS**

- **pass** — replaying a delivered message sends nothing further `before=1 after=1`
- **pass** — replaying a created CRM record creates nothing further
- **pass** — each skip explains itself against the claim `effect already delivered | effect already applied`
- **pass** — a permanent failure was dead-lettered
- **pass** — its claim was marked failed rather than succeeded
- **pass** — the operator replay cleared only the failed claim
- **pass** — the effect then completed
- **pass** — the dead letter is marked replayed
- **pass** — and the previously delivered message was still not re-sent `expected exactly one new send, got 1`
